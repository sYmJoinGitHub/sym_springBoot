## springData JPA整合
spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: ture
        show-sql: true
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type: TRACE